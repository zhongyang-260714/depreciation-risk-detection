# Streamlit 主题配置 —— 与 app.py 中的 CSS 配色保持一致
# 低饱和暖色系：深青主色 + 暖白背景

[theme]
primaryColor = "#17705C"
backgroundColor = "#FAF8F5"
secondaryBackgroundColor = "#F1EEE8"
textColor = "#2B2B28"
font = "sans serif"
